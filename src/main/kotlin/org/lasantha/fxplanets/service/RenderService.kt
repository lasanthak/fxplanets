package org.lasantha.fxplanets.service

import javafx.scene.canvas.GraphicsContext

class RenderService(private val gc: GraphicsContext, private val gameService: GameService) {

    fun update(time: Long) {

    }
}
